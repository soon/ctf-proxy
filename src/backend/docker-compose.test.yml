services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ctf
      POSTGRES_PASSWORD: ctf
      POSTGRES_DB: ctf
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ctf"]
      interval: 2s
      timeout: 5s
      retries: 15
